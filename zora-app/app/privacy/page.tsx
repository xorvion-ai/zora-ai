// /privacy — Privacy policy (static).

import { PrivacyScreen } from '@/components/screens/about-contact-terms';

export default function PrivacyPage() {
  return (
    <div style={{ minHeight: '100dvh', background: 'var(--bg-0)' }}>
      <PrivacyScreen width="100vw" height="100dvh" />
    </div>
  );
}
