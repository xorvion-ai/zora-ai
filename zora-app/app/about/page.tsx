// /about — About Zora / Xorvion (static).

import { AboutScreen } from '@/components/screens/about-contact-terms';

export default function AboutPage() {
  return (
    <div style={{ minHeight: '100dvh', background: 'var(--bg-0)' }}>
      <AboutScreen width="100vw" height="100dvh" />
    </div>
  );
}
