// /pricing — Free / Lite / Pro plan comparison (static).

import { PricingScreen } from '@/components/screens/about-contact-terms';

export default function PricingPage() {
  return (
    <div style={{ minHeight: '100dvh', background: 'var(--bg-0)' }}>
      <PricingScreen width="100vw" height="100dvh" />
    </div>
  );
}
