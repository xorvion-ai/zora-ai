// /terms — Terms & conditions (static).

import { TermsScreen } from '@/components/screens/about-contact-terms';

export default function TermsPage() {
  return (
    <div style={{ minHeight: '100dvh', background: 'var(--bg-0)' }}>
      <TermsScreen width="100vw" height="100dvh" />
    </div>
  );
}
